{
  "name": "recipes-md",
  "version": "1.0.0",
  "private": true,
  "description": "Recipe collection PWA built from Markdown files",
  "license": "ISC",
  "workspaces": [
    "app",
    "packages/*"
  ],
  "scripts": {
    "check": "biome check .",
    "format": "biome check --write .",
    "typecheck": "npm run typecheck --workspace packages/design-system && npm run typecheck --workspace app",
    "build": "npm run build --workspace app",
    "dev": "npm run dev --workspace app"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.3.0"
  }
}
